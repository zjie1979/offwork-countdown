# 下班倒计时

一个可公开部署到 GitHub Pages 的 iPhone PWA，用于显示中午和下午两段下班倒计时。

## 功能

- 默认中午下班 `12:00`，下午下班 `18:00`。
- 当天未到中午时显示中午倒计时，过了中午后自动切到下午倒计时。
- 过了下午下班后显示下一次中午下班倒计时。
- 可设置每日摸鱼额度，点击开始后自动正计时，暂停后保留今天累计时间。
- 下班时间、计时日期和摸鱼记录保存在本机浏览器，不上传服务器。

## 发布范围

本仓库只包含静态网页文件，不包含个人记录、账号、密钥或业务数据。
